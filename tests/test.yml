---
- hosts: all
  become: yes
  roles:
    - role: ansible-role-docker
      vars:
        docker_user:
          - vagrant
        include_docker_volume_netshare: false
    - role: ansible-role-kubernetes
