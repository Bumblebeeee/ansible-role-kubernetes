---
# vars file for install-kubernetes
docker:
  include_docker_volume_netshare: false
kubernetes:
  packages:
    - kubectl
    - kubeadm
    - kubelet
    - python3-pip

  pre_packages:
    - apt-transport-https
    - ca-certificates
    - curl
    - gnupg
